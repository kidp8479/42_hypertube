// Protected `/` route: the landing page after login (shows the current user
// and a logout button). Stub for now - filled in later this issue.
export function HomePage() {
  return <p>Home Page</p>;
}
