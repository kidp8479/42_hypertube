// Public /login route: email + password form that calls the context `login()`
// and redirects to `/` on success. Stub for now - filled in later this issue.
export function LoginPage() {
  return <p>Login Page</p>;
}
